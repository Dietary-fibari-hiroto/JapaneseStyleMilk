import {
  CountDown,
  DebateButton,
  PopUpUserCard,
  VoiceToChat,
  Loading,
  SecondaryButton,
  ResultCard,
} from "../components";

const Test = () => {
  return (
    <div className="bg-[#cccccc]">
      <ResultCard win={true} />
    </div>
  );
};

export default Test;
