import { Routes, Route, useLocation } from "react-router-dom";
import { AnimatePresence } from "framer-motion";

import Home from "./pages/Home";

const routeList = [{ path: "/", element: <Home /> }];

const routes = () => {};

export default routes;
